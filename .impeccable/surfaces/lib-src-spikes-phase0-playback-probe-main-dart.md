---
version: 1
slug: "lib-src-spikes-phase0-playback-probe-main-dart"
primary_target: "lib/src/spikes/phase0/playback_probe_main.dart"
related_targets: ["lib/src/spikes/phase0/strong_playback_probe.dart","lib/src/spikes/phase0/strong_probe_models.dart"]
---

## Scope and mode

Temporary Phase 0 Windows diagnostic entrypoint only. Mode: Operate. It is not reachable from the ordinary Wabbit TV app and must not establish production source setup, playback controls, multiview UI, or reusable architecture.

## Audience and job

The maintainer enters one Strong account locally, selects one bounded representative live item, movie, and episode, then proves packaged Windows rendering and records a sanitized result. The task must remain obvious under mouse and keyboard without exposing provider identity or secret-bearing values.

## Chosen direction

Use approved composition B, Split Console: a fixed left task console for Account, Select, Playback, and Results; a flexible right mounted 16:9 stage with a compact telemetry strip, three-row evidence ledger, and explicit optional two-stream admission. The memorable moment is the real video frame appearing beside immediately legible sanitized facts, not product chrome.

Approved comp: `.impeccable/mocks/quiet-broadcast-playback-probe-b-split-console.png`.

## Fidelity boundary

Preserve the comp's split hierarchy, quiet density, mounted stage dominance, and single amber immediate action. Do not literalize illustrative test-pattern graphics, synthetic timings, connection counts, sample titles, or success states. Before account validation, credentials occupy the left console; after validation, that section collapses to sanitized facts. Provider selections may show real titles only transiently inside the live local window and must never enter copied/durable evidence. The implementation may use simple Flutter-native controls where exact comp recreation would add machinery to this disposable spike.

## Constraints

Memory-only credentials; no persistence, command-line secrets, logs, screenshots, raw JSON, URLs, IDs, provider identity, titles, or raw engine errors in copied evidence. One muted player at a time for sequential proof, disposed before the next. Two-stream appears only when known available allowance is at least two and the maintainer explicitly runs it. Account request and playback actions are bounded and have no automatic retry. Full Tab order, Enter activation, visible focus, and safe Escape behavior are required.

## Unresolved decisions

None that block implementation. Real playback evidence remains user-local and cannot be claimed until the maintainer runs the packaged probe with their credentials.
